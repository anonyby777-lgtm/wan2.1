# novo-projeto

Projeto inicial pronto para subir no GitHub.

## Como usar

```bash
git init
git add .
git commit -m "primeiro commit"
gh repo create novo-projeto --public --source=. --push
```

## Estrutura
- `src/` — código
- `tests/` — testes
- `.github/workflows/ci.yml` — CI no GitHub Actions
